package week03;

public class _Ternaries {
    public static void main(String[] args) {

        // ternaries is a short version of IF  statments
        // single if can not be turned to ternaries, since number of ? must be == to number of :
        // return value should be the same data type!
        // returning only a single statements
        // even tho ternaries is shorter it's less readable + less use cases

        // syntax:
        // (condition) ? StatementA : StatementB ;

    }
}
