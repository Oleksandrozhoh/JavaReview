package week03;

public class IfStatements {
    public static void main(String[] args) {

        // if you have a code fragment that has to be executed under a sertain condition we use IF statments


        //Single IF:
        //if(condition){statements}

        // IF & ELSE: 2 possible outputs - not more, not less
        /*
        if(condition){
            Statements
        }else{
            Statements
        }
         */

        // Multibranch IF: for 3+ possible outputs:
        /*
        if(conditionA){
        statementsA
        }else if(conditionB){
        statementsB
        }else{
        statementC
        }
         */

        // Nested IF statment: for preconditions
        /*
        if(Pre-condition){
           if statments || IF&ELSE || Multibranch IF
           }else{
           if statments || IF&ELSE || Multibranch IF
           }
         */

    }
}
